name: Verify automatic schedule

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm automatic run
        run: echo "Automatic schedule works"
